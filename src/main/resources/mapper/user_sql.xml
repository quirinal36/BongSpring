<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user_sql">
	<insert id="insert" parameterType="user">
	INSERT INTO users 
	(
		username,
		password,
		user_phon
	)
	values
	(
		'${username}',
		'${password}',
		'${user_phon}'
	)
	</insert>
	<select id="selectOne" parameterType="user" resultType="user">
		SELECT 
			*
		FROM users 
		<where>
			<if test="user_phon != null">user_phon = '${user_phon}'</if>
		</where>
	</select>
	
	<select id="login" parameterType="user" resultType="user">
		SELECT 
			usr.id, 
			usr.username, 
			usr.password, 
			role.role AS role
		FROM users usr, user_roles role
		<where>
			usr.username = '${username}'
			and usr.user_role = role.id
		</where>
	</select>
	
	<select id="select_all" resultType="user">
		SELECT 
			usr.id,
			usr.username,
			usr.user_phon,
			usr.user_role,
			role.role AS role
		FROM users usr left join user_roles role on usr.user_role = role.id
		<where>
			
			
		</where>
		order by username desc
	</select>
	
	<update id="update" parameterType="user" >
		UPDATE users
		<set>
			<choose>
				<when test="username != null and username!= ''">
				username = '${username}',
				</when>
				<when test="password != null and password != ''">
				password = '${password}',
				</when>
				<otherwise>
				user_role = ${user_role}
				</otherwise>
			</choose>
		</set>
		<where>
			id = ${id}
		</where>
	</update>
</mapper>
